# OOP-LAB-TASK-10
These tasks demonstrate basic file handling in C++ using Object-Oriented Programming (OOP). The programs perform different file operations such as creating, writing, reading, appending, counting lines, and copying data between files. Classes, objects, and member functions are used to organize and perform these operations efficiently.
